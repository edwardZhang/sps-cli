{
  "name": "@jarvis/workflow-cli",
  "version": "0.1.0",
  "description": "JARVIS Workflow CLI — AI-driven development pipeline orchestrator",
  "type": "module",
  "main": "dist/main.js",
  "bin": {
    "workflow": "../bin/workflow"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
